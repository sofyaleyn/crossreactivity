"""CrossFlag reference-set build pipeline.

Assembles the layered self-protein reference set (layers A/B/C + background)
consumed by the embedding pipeline. See docs/reference-set.md for the design
and order of operations; `build_set.py` is the orchestrator entry point.
"""
