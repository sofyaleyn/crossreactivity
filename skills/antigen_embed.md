---
name: antigen_embed
description: Embed a self-protein antigen (ESM-2, surface region or sliding window) into vector(s). STUB.
---

# antigen_embed (STUB)

Wraps `crossflag.embed.antigen.embed_antigen`. See HANDOFF Step 4 / docs/mvp-spec.md §[3].

- **Input:** `{sequence: str, surface_region: list[int] | None}`
- **Output:** `np.ndarray | list[np.ndarray]`.

_Fill in exact schema + invocation when implemented (HANDOFF Step 12)._
